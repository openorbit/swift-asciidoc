<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ headerTitle }}</title>
    <style>
        body { display: flex; margin: 0; font-family: sans-serif; }
        nav { width: 300px; background: #f8f9fa; padding: 1rem; border-right: 1px solid #ddd; height: 100vh; overflow-y: auto; position: fixed; }
        main { margin-left: 300px; padding: 2rem; max-width: 800px; }
        
        ul.nav-list { list-style: none; padding-left: 0; }
        ul.nav-list li { margin-bottom: 0.5rem; }
        ul.nav-list ul { padding-left: 1rem; }
        a { text-decoration: none; color: #007bff; }
        a:hover { text-decoration: underline; }
        .nav-link { display: block; padding: 0.2rem 0; }
        .nav-link.active { font-weight: bold; color: #0056b3; }
        
        /* AsciiDoc Styling Helpers */
        .admonitionblock { margin-bottom: 1.25em; border-left: 5px solid #dddddd; padding: 0.5em; background-color: #f8f8f8; }
        .admonitionblock td.icon { width: 40px; text-align: center; }
        .listingblock { margin-bottom: 1.25em; }
        pre { background: #f5f5f5; padding: 1em; overflow-x: auto; border-radius: 4px; }
    </style>
</head>
<body>

<nav>
    <ul class="nav-list">
        {% for item in navigation.flatItems %}
        <li style="padding-left: calc({{ item.depth }} * 1rem);">
            {% if item.href %}
                <a href="{{ item.href }}" class="nav-link">{{ item.label }}</a>
            {% else %}
                <span class="nav-link">{{ item.label }}</span>
            {% endif %}
        </li>
        {% endfor %}
    </ul>
</nav>

<main>
    <h1>{{ headerTitle }}</h1>
    
    {% for block in blocks %}
        {% include "html5/block.stencil" %}
    {% endfor %}
    
    {% if footnotes %}
    <div id="footnotes">
        <hr>
        {% for fn in footnotes %}
        <div class="footnote" id="_footnotedef_{{ fn.id }}">
            <a href="#_footnoteref_{{ fn.id }}">{{ fn.id }}</a>. {{ fn.content }}
        </div>
        {% endfor %}
    </div>
    {% endif %}
</main>

</body>
</html>
